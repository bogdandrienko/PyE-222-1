# cmd
python -m venv env

# syntax
# junior: types, operators, cond, loops, func, oop, datetime, file / dirs
# light complex practic - excel reader / weather parser / txt writer / json serialize
# middle: sugar syntax (более короткие и ёмкие команды), virt, decorators, exception, oop
# intermediate: async, thread, oop, regex